---
layout: default
title: Homepage
tags: page
---

<h1>{{ title }}</h1>

<h2>test</h2> 

<img src="/assets/images/cms/placeholder.svg" alt="">


<ul>
    {%- for post in collections.all -%}
      <li{% if page.url == post.url %} aria-current="page"{% endif %}>
        <a href="{{post.url}}">{{ post.data.title }}</a>
    </li>
    {%- endfor -%}
</ul>
